{
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp -r public/assets build/",
    "vercel-build": "npm run build && mv build public",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}